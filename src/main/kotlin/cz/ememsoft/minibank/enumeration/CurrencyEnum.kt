package cz.ememsoft.minibank.enumeration

/**
 * Enumeration of supported currencies.
 *
 * This enum provides type safety for currency codes used in accounts and transactions.
 * Extend this enum as needed to support additional currencies.
 */
enum class CurrencyEnum {
    USD,
    EUR,
    GBP
    // Add other currencies as needed.
}
